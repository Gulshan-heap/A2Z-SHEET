class Solution {
public:
    double myPow(double x, int n) {
        // base case
        if(n==0)    return 1;
        double ans;
        if(n>0) ans = x* pow(x,n-1);
        else if(n<0)   ans = 1/x*pow(x,n+1);
        return ans;
    }
};
